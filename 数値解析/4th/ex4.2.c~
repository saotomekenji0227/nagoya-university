#include <stdio.h>
int main(int argc,char* argv[]){
  int i;
  double x=-1.0;
  double a1[9];
  double a2[11];
  double x1[9];
  double x2[11];
  for(i=0;i<9;i++){
    xx[i]=x;
    a1[i]=f(x);
    x+=1/4;
  }
  x=-1.0;
  for(i=0;i<11;i++){
    xx[i]=x;
    a2[i]=f(x);
    x+=1/5;
  }
  g(1,a1);
  return 0;
  
}
double f(double x){
  return 1/(1+25*x*x);
}
double g(double num,a[]){
  return 0;
}
